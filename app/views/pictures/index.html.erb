<h1>ブログ一覧</h1>

<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @pictures.each do |pct| %>
  <tr>
    <td><%= pct.content %></td>
    <td><%= pct.picture %></td>
  </tr>
<% end %>
</table>
<%= link_to '投稿する', new_picture_path %>
