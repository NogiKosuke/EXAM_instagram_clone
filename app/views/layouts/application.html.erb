<!DOCTYPE html>
<html>
  <head>
    <title>Telegram</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <<body>
    <header class = 'header'>
      <ul class = "clearfix">
        <% if logged_in? %>
          <li class = "header_menu"><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
          <li class = "header_menu"><%= link_to "Profile", user_path(current_user.id) %></li>
        <% else %>
          <li class = "header_menu"><%= link_to "Sign up", new_user_path %></li>
          <li class = "header_menu"><%= link_to "Login", new_session_path %></li>
        <% end %>
      </ul>
    </header>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
      <%= yield %>
  <footer>
  </footer>
  </body>
</html>
